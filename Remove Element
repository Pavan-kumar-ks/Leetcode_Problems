class Solution {
    public int removeElement(int[] nums, int val) {
        
        int i,j,n,c=0;
        n = nums.length;
        for(i=0;i<n;i++)
        {
            if(nums[i] != val){
                nums[c] = nums[i];
                c++;
            }
        }
        return c;
    }
}
